spring.application.name=socialmediaapp
spring.profiles.active=dev

app.jwt.secret=${JWT_SECRET:dev-only-jwt-signing-secret-change-this-before-any-real-deployment-1234567890}
app.jwt.access-token-expiration-minutes=15
app.jwt.refresh-token-expiration-days=7

eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8761/eureka}

spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}
